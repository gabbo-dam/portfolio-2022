import type { NextPage } from 'next';
import { ButtonProps } from '../types';

const Outlined: NextPage<ButtonProps> = () => {
  return (
    <div>
      <button>Outlined</button>
    </div>
  );
};

export default Outlined;
