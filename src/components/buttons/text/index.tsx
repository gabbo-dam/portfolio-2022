import type { NextPage } from 'next';
import { ButtonProps } from '../types';

const Text: NextPage<ButtonProps> = () => {
  return (
    <div>
      <button>Text</button>
    </div>
  );
};

export default Text;
